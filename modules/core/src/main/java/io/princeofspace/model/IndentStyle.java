package io.princeofspace.model;

/** Controls whether indentation uses spaces or tab characters. */
public enum IndentStyle {
    /** Use ASCII space characters for each indent step. */
    SPACES,
    /** Use tab characters for each indent step. */
    TABS
}
